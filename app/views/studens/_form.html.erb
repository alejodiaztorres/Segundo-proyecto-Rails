<%= form_with(model: studen, local: true) do |form| %>
  <% if studen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(studen.errors.count, "error") %> prohibited this studen from being saved:</h2>

      <ul>
      <% studen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
